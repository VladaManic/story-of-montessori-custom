<section id="featur-prods">
  <div class="container-custom">
    <div class="row">
      <h2 class="{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--slide-in{% endif %}">
        {{ section.settings.title }}
      </h2>
      <div class="featur-wrap">
        {% assign productsByDate = section.settings.featured_collection.products | sort: 'date' | reverse %}
        {% for product in productsByDate %}
          {% render 'custom-product-cart', product_product: product, index: forloop.index %}
        {% endfor %}
      </div>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Featured products new",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Title"
    },
    {
      "type": "collection",
      "id": "featured_collection",
      "label": "Collection"
    }
  ],
  "presets": [
    {
      "name": "Featured products new"
    }
  ]
}
{% endschema %}
